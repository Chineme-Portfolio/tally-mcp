# Helm — UI Rules

> **Status: PENDING.** Awaits the Claude Design export (Phase 3). Do not hand-invent rules.

This file will define how tokens compose into UI: a **§0 prime directive** (the one sentence every screen must serve, derived from Helm's brand voice in `foundation.md`), then layout/density, hierarchy, color discipline, per-surface rules (here: the launch-board widget), and required interaction states (checked/unchecked, dragging, empty, error).

**Until it exists:** keep the widget clean, legible, and neutral (`code-standards.md` §7). Generated alongside `ui-tokens.md` and `ui-registry.md` from the design export.
